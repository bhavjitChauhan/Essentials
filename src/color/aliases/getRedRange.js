/**
 * Gets the color range for red.
 * 
 * @category Color
 * 
 * @returns {number}
 * 
 * @example
 * colorMode(RGB, 1, 1, 1);
 * println(getRedRange());
 * // expected output: 1
 * 
 * @see {@link getColorRange}
 */
getRedRange = () => p.red(WHITE);
