/**
 * Gets the color range for blue.
 * 
 * @category Color
 * 
 * @returns {number}
 * 
 * @example
 * colorMode(RGB, 1, 1, 1);
 * println(getBlueRange());
 * // expected output: 1
 * 
 * @see {@link getColorRange}
 */
getBlueRange = () => p.blue(WHITE);
