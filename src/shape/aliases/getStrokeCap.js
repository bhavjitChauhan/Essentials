/**
 * Gets current stroke cap.
 * 
 * @category Shape
 * 
 * @returns {string}
 * 
 * @example
 * strokeCap(SQUARE);
 * println(getStrokeCap() == SQUARE);
 * // expected output: true
 */
getStrokeCap = () => ctx.lineCap;
