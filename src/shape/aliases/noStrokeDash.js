/**
 * Removes line dash pattern.
 * 
 * @category Shape
 * 
 * @example
 * strokeDash(1, 20);
 * noStrokeDash();
 * line(50, 50, 350, 50);
 * // expected outcome: regular line
 * 
 * @see {@link strokeDash}
 */
noStrokeDash = () => strokeDash();
